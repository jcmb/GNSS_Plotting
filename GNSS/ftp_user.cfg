# Legacy shim: sets FTP_USER, FTP_SERVER, FTP_BACKUP_PATH for TCC upload.
# Server list and jobs are defined in ftp_servers.cfg.
_LFTP_DIR=`cd "$(dirname "${BASH_SOURCE[0]}")" && pwd`
. "$_LFTP_DIR/ftp_lib.sh"
ftp_load_config "$_LFTP_DIR"
ftp_resolve_server "$FTP_TCC_UPLOAD_SERVER"
